# Placeholder Dockerfile for Django backend
# TODO: replace with actual build once backend scaffolding exists
FROM python:3.11-slim
WORKDIR /app
COPY . /app
RUN echo "Backend dependencies/install steps go here"
CMD ["python", "manage.py", "runserver", "0.0.0.0:8000"]
