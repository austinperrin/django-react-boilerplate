# Django + React Boilerplate

This repository is a **Django + React (Vite) + Docker** boilerplate intended as a starting point for
enterprise‑grade, scalable, and security‑focused systems. The backend and frontend are **fully decoupled**
(separate servers), and the structure is designed to grow into a monorepo with additional services.

---
